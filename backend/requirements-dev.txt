-r requirements.txt

# --- Model training / EDA (never imported by the running API) ---
pandas==2.2.3

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1                 # FastAPI TestClient transport
